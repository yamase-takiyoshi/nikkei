+  else if(/保険業|保険/.test(industry))p={...base,kind:'insurance',revenueLabel:'経常収益',revenueShort:'経常収益',primaryProfitKey:'ordinaryProfit',primaryProfitLabel:'経常利益',primaryProfitShort:'経常益',primaryMarginLabel:'経常利益率',revenueGrowthLabel:'経常収益成長率',enterpriseToRevenueLabel:'EV/経常収益',revenuePerShareLabel:'1株当たり経常収益'};
+  else if(/証券.*商品先物|証券|商品先物/.test(industry))p={...base,kind:'securities',revenueLabel:'営業収益',revenueShort:'営業収益',revenueGrowthLabel:'営業収益成長率',enterpriseToRevenueLabel:'EV/営業収益',revenuePerShareLabel:'1株当たり営業収益'};
+  else if(/その他金融業/.test(industry))p={...base,kind:'finance',revenueLabel:'営業収益',revenueShort:'営業収益',revenueGrowthLabel:'営業収益成長率',enterpriseToRevenueLabel:'EV/営業収益',revenuePerShareLabel:'1株当たり営業収益'};
+  else if(/REIT|不動産投資信託|投資法人/.test(industry))p={...base,kind:'reit',revenueLabel:'営業収益',revenueShort:'営業収益',revenueGrowthLabel:'営業収益成長率',enterpriseToRevenueLabel:'EV/営業収益',revenuePerShareLabel:'1口当たり営業収益'};
+
+  // 決算表の実際の項目名を業種推定より優先。IFRSや金融業の表記差を会社ごとの手修正なしで吸収する。
+  if(sourceRevenueLabel){
+    p={...p,revenueLabel:sourceRevenueLabel,revenueShort:sourceRevenueLabel,labelBasis:'source',
+      revenueGrowthLabel:`${sourceRevenueLabel}成長率`,enterpriseToRevenueLabel:`EV/${sourceRevenueLabel}`,
+      revenuePerShareLabel:p.kind==='reit'?`1口当たり${sourceRevenueLabel}`:`1株当たり${sourceRevenueLabel}`};
+  }
+  // 銀行・保険は経常利益を優先。それ以外は一次データの見出し、次に実データの有無から主要利益を選ぶ。
+  if(!/bank|insurance/.test(p.kind)){
+    const chosen=sourcePrimary || (!hasOperating&&hasOrdinary?{key:'ordinaryProfit',label:'経常利益',short:'経常益',margin:'経常利益率'}:null);
+    if(chosen)p={...p,primaryProfitKey:chosen.key,primaryProfitLabel:chosen.label,primaryProfitShort:chosen.short,primaryMarginLabel:chosen.margin,profitLabelBasis:sourcePrimary?'source':'data'};
+  }
+  return p;
+}
+function companyPrimaryProfit(row,profile){return row?.[profile?.primaryProfitKey||'operatingProfit'];}
+function companyPrimaryMarginValue(prof,profile){return (profile?.primaryProfitKey==='ordinaryProfit')?prof?.ordinaryMargin:prof?.operatingMargin;}
+
 function renderCompanySalesCandles(){
   const canvas=$('companySalesCandleChart');if(!canvas)return;
   if(charts.companySalesCandleChart){charts.companySalesCandleChart.destroy();delete charts.companySalesCandleChart;}
   const c=(dashboardState.companyData||[]).find(x=>String(x.code)===String(selectedCompanyCode));
+  const detail=c?.fullDetail||{},accounting=getCompanyAccountingProfile(c,detail.corporate||{},detail),revenueLabel=accounting.revenueLabel;
   const scope=companyPeriodScope==='quarterly'?'quarterly':'annual',quarterly=scope==='quarterly';
   const comparison=quarterly?companySalesComparison:'previous';
   const refLabel=quarterly?(comparison==='yearAgo'?'前年同期':'前四半期'):'前期';
@@ -1528,15 +1579,20 @@
   $('companySalesComparison').classList.toggle('hidden',!quarterly);
   $('companySalesScope').textContent=scopeLabel+'・実績';
   $('companySalesCompany').textContent=c?`${c.code} ${c.name}`:'企業を選択してください';
+  if($('companySalesHeading'))$('companySalesHeading').textContent=`${revenueLabel}推移・ローソク形`;
+  if($('companySalesDetailsSummary'))$('companySalesDetailsSummary').textContent=`${revenueLabel}の数値を確認`;
+  const salesControls=document.querySelector('.company-sales-controls[aria-label]');
+  if(salesControls)salesControls.setAttribute('aria-label',`${revenueLabel}の表示期間`);
+  canvas.setAttribute('aria-label',`${revenueLabel}実績を前期または前年同期と比較するローソク形グラフ`);
+  canvas.textContent=`下の「${revenueLabel}の数値を確認」に各期の${revenueLabel}と増減率を掲載しています。`;
   const update=$('companySalesUpdate');update.disabled=!c||!!c.detailLoading;
-  update.textContent=c?.detailLoading?'取得中…':'売上高を更新';
-  const detail=c?.fullDetail||{};
+  update.textContent=c?.detailLoading?'取得中…':`${revenueLabel}を更新`;
   const input=quarterly?(detail.quarterlyPerformance||[]):(c?.performance?.length?c.performance:(detail.annualPerformance||[]));
   const all=buildCompanySalesCandles(input,scope,comparison),rows=all.slice(-20);
-  $('companySalesExplanation').textContent=`四角の両端は「${refLabel}の売上高」と「当期の売上高」です。実際の高値・安値を示すヒゲはありません。比較値がない期は当期の金額を点で表示します。単位：百万円。`;
+  $('companySalesExplanation').textContent=`四角の両端は「${refLabel}の${revenueLabel}」と「当期の${revenueLabel}」です。実際の高値・安値を示すヒゲはありません。比較値がない期は当期の金額を点で表示します。単位：百万円。`;
   const hasSales=rows.some(r=>isNum(r.sales));
   const empty=$('companySalesCandleEmpty');
-  empty.textContent=!c?'企業を選択してください。':!hasSales?(c.detailLoading?'売上高を取得中…':`${scopeLabel}の売上高実績がありません。「売上高を更新」で取得できます。`):'';
+  empty.textContent=!c?'企業を選択してください。':!hasSales?(c.detailLoading?`${revenueLabel}を取得中…`:`${scopeLabel}の${revenueLabel}実績がありません。「${revenueLabel}を更新」で取得できます。`):'';
   const latest=rows.filter(r=>isNum(r.sales)).at(-1);
   $('companySalesSummary').innerHTML=latest?`<strong>${esc(latest.period)}　${esc(companyFmtMoney(latest.sales))}</strong><span>${esc(refLabel)}比 ${companyPctHtml(latest.changeRate)}</span>${quarterly&&comparison!=='yearAgo'?`<span>前年同期比 ${companyPctHtml(latest.yearAgoRate)}</span>`:''}`:'';
   $('companySalesSummary').classList.toggle('hidden',!latest);
@@ -1546,27 +1602,27 @@
   const source=quarterly?(detail.source||'出典情報なし'):(detail.source||c?.source||'出典情報なし');
   if(c)notes.push(`出典：${source}${detail.fetchedAt?' ／ 取得 '+new Date(detail.fetchedAt).toLocaleString('ja-JP',{timeZone:'Asia/Tokyo'}):c.updated?' ／ 確認 '+c.updated:''}`);
   $('companySalesSource').textContent=notes.filter(Boolean).join(' ');
-  $('companySalesTable').innerHTML=`<caption class="company-sales-caption">${esc(c?c.code+' '+c.name:'売上高')} ／ ${esc(scopeLabel)}売上高・実績 ／ 単位：百万円</caption><thead><tr><th>期間</th><th>当期売上高</th><th>${esc(refLabel)}売上高</th><th>増減額</th><th>${quarterly?'前四半期比':'前年比'}</th>${quarterly?'<th>前年同期比</th>':''}</tr></thead><tbody>${rows.length?rows.map(r=>`<tr><td>${esc(r.period)}</td><td>${isNum(r.sales)?esc(fmt(r.sales,0)):'未取得'}</td><td>${isNum(r.referenceSales)?esc(fmt(r.referenceSales,0)):'未取得'}</td><td class="${r.direction}">${isNum(r.change)?(r.change>0?'+':'')+esc(fmt(r.change,0)):'比較不可'}</td><td>${companyPctHtml(r.previousRate)}</td>${quarterly?`<td>${companyPctHtml(r.yearAgoRate)}</td>`:''}</tr>`).join(''):`<tr><td colspan="${quarterly?6:5}">売上高実績を取得できていません。</td></tr>`}</tbody>`;
+  $('companySalesTable').innerHTML=`<caption class="company-sales-caption">${esc(c?c.code+' '+c.name:revenueLabel)} ／ ${esc(scopeLabel)}${esc(revenueLabel)}・実績 ／ 単位：百万円</caption><thead><tr><th>期間</th><th>当期${esc(revenueLabel)}</th><th>${esc(refLabel)}${esc(revenueLabel)}</th><th>増減額</th><th>${quarterly?'前四半期比':'前年比'}</th>${quarterly?'<th>前年同期比</th>':''}</tr></thead><tbody>${rows.length?rows.map(r=>`<tr><td>${esc(r.period)}</td><td>${isNum(r.sales)?esc(fmt(r.sales,0)):'未取得'}</td><td>${isNum(r.referenceSales)?esc(fmt(r.referenceSales,0)):'未取得'}</td><td class="${r.direction}">${isNum(r.change)?(r.change>0?'+':'')+esc(fmt(r.change,0)):'比較不可'}</td><td>${companyPctHtml(r.previousRate)}</td>${quarterly?`<td>${companyPctHtml(r.yearAgoRate)}</td>`:''}</tr>`).join(''):`<tr><td colspan="${quarterly?6:5}">${esc(revenueLabel)}実績を取得できていません。</td></tr>`}</tbody>`;
   if(!hasSales)return;
-  if(typeof Chart==='undefined'){empty.textContent='グラフを読み込めませんでした。下の「売上高の数値を確認」で数値をご覧いただけます。';return;}
+  if(typeof Chart==='undefined'){empty.textContent=`グラフを読み込めませんでした。下の「${revenueLabel}の数値を確認」で数値をご覧いただけます。`;return;}
   const css=getComputedStyle(document.documentElement),color=name=>css.getPropertyValue(name).trim();
   const colors=rows.map(r=>color(r.direction==='up'?'--up':r.direction==='down'?'--down':'--flat'));
   const defaults=chartDefaults();
   charts.companySalesCandleChart=new Chart(canvas,{
     type:'bar',plugins:[companySalesCandleLabels(rows)],
     data:{labels:rows.map(r=>r.period),datasets:[
-      {label:'売上高の増減',data:rows.map(r=>r.body),backgroundColor:colors,borderColor:colors,borderWidth:1,borderSkipped:false,barPercentage:.56,categoryPercentage:.85,maxBarThickness:32,minBarLength:2,order:1},
-      {type:'line',label:'当期売上高（比較値なし）',data:rows.map(r=>!r.body?r.sales:null),showLine:false,pointRadius:4,pointHoverRadius:6,pointBackgroundColor:color('--accent'),pointBorderColor:color('--panel'),pointBorderWidth:1,order:0}
+      {label:`${revenueLabel}の増減`,data:rows.map(r=>r.body),backgroundColor:colors,borderColor:colors,borderWidth:1,borderSkipped:false,barPercentage:.56,categoryPercentage:.85,maxBarThickness:32,minBarLength:2,order:1},
+      {type:'line',label:`当期${revenueLabel}（比較値なし）`,data:rows.map(r=>!r.body?r.sales:null),showLine:false,pointRadius:4,pointHoverRadius:6,pointBackgroundColor:color('--accent'),pointBorderColor:color('--panel'),pointBorderWidth:1,order:0}
     ]},
